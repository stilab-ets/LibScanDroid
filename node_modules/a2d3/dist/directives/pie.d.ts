import { ElementRef } from '@angular/core';
import { D3ChartDirective, D3Element, D3MarginDirective } from './chart';
import * as d3 from 'd3';
export declare class D3PieBase extends D3Element {
    innerRadius: number;
    labelRadius: number;
    radiusAttr: string;
    readonly radius: number;
    createArc(radius: number): d3.svg.Arc<any>;
    createLabelArc(radius: any, offset?: number): d3.svg.Arc<any>;
    createArcTween(arc: any, initial?: any): (d: any) => (t: any) => any;
    redraw(): void;
}
export declare class D3PieDirective extends D3PieBase implements D3Element {
    name: string;
    transition: string;
    duration: number;
    value: string;
    label: string;
    innerRadius: number;
    labelRadius: number;
    labelOffset: number;
    textAnchor: string;
    color: string;
    avoidCollisions: boolean;
    dy: string;
    pie: d3.layout.Pie<number>;
    private _center;
    private _colorScale;
    constructor(chart: D3ChartDirective, el: ElementRef, margin?: D3MarginDirective);
    colorScale: any;
    redraw(): void;
    private getLabelPosition(radius);
    private getColors(d, i);
}
